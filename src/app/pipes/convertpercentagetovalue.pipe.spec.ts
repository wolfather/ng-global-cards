import { ConvertpercentagetovaluePipe } from './convertpercentagetovalue.pipe';

describe('ConvertpercentagetovaluePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertpercentagetovaluePipe();
    expect(pipe).toBeTruthy();
  });
});

import { InputallownumbersDirective } from './inputallownumbers.directive';

describe('InputallownumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new InputallownumbersDirective();
    expect(directive).toBeTruthy();
  });
});
